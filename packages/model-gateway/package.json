{
  "name": "@repo/model-gateway",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "zod": "^3.0.0",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
